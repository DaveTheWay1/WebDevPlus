// * Two Ways toe create arrays

// using Array Literal Notation (recommended best practice)
const nums1 = [2, 4, 18];

// using the Array class (less common approach)
const nums2 = new Array(2, 4, 18);

// The best practice is to use Array Literal Notation because 
// it’s more concise and the class approach behaves differently 
// if you pass only one argument.