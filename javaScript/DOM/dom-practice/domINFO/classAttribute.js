// * The class Attribute

// * classList is an object with the following methods pertaining to classes:
// add(className, className, ...)
// remove(className, className, ...)
// toggle(className)
// contains(className)
// replace(oldClass, newClass)

// ? If we want to change the text (without HTML) inside of a <div>, 
// which property should we assign to?
// innerText
// ? How many DOM elements are returned by the querySelector method?
// one
// ? What DOM element property is used to style a DOM element using JS?
// style