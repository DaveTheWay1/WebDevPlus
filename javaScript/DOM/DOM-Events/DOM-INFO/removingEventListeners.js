// * Removing Event Listeners

// * Although not usually necessary, it’s possible to remove an event listener, 
// but only if a named function was used as the callback.

btn.removeEventListener('click', handleClick);