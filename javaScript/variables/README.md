let vs. const
The difference between let and const is that a variable declared using const cannot be reassigned to after its initialization.

let and var variables 
can be updated after they are declared