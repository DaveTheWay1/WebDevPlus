<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact</title>
</head>
<body>
    <%- include('partials/nav.ejs') %>
    <h1>David Ramirez-Vazquez</h1>
    <p>Phone Number:</p>
    <p>Email:</p>
    <p> <%= qs.name %> </p>

    <form id="contact-form" action="">
        <label for="who">Who do you want to contact?</label>
        <input type="text" name="who" value="<%= qs.name %> ">
        <!-- assuming we put the below on search browser -->
        <!-- http://localhost:3000/contact?name=david&dept=cs -->
        <!-- it creates an object of name and dept
        hence why we are able to then use the %= to display .name and .dept-->
        <!-- the qs comes from the app.js which stores query string (req.query) -->
        <label for="department">Which department?</label>
        <input type="text" name="department" value="<%= qs.dept %> ">
        <label for="email">Your email</label>
        <input type="email" name="email">
        <input type="submit" value="submit">
    </form>
</body>
</html>